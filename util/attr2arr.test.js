import test from 'node:test';
import assert from 'node:assert/strict';

import attr2arr from './attr2arr.js';

console.log(attr2arr("uno|dos|tres|cuatro"));

// test('attr2arr works', (t) => {
  
//   let res
//   assert(tmp.data.zero === 0 && tmp.data.one === 11)
  
// });

